AWSTemplateFormatVersion: '2010-09-09'
Description: >
  API Gateway placeholder (REST API + usage plan).
  Implemented in Phase 5.

# Placeholder stack: declares the Stage interface it will use in Phase 5
# so the root template's parameter flow is stable now.
Metadata:
  cfn-lint:
    config:
      ignore_checks:
        - W2001

Parameters:
  Stage:
    Type: String
    AllowedValues:
      - dev
      - prod

Resources:
  Placeholder:
    Type: AWS::CloudFormation::WaitConditionHandle
