# DEPRECATED: This workflow has been replaced by ci-cd.yml
# 
# The new CI/CD pipeline includes:
# - Comprehensive testing (unit, property-based, integration)
# - Code quality checks (linting, type checking, security scanning)
# - Blue-green deployment with automatic rollback
# - Lambda Layer deployment
# - Step Functions and API Gateway deployment
# - Deployment notifications
#
# Please use .github/workflows/ci-cd.yml instead
#
# This file is kept for reference only and will be removed in a future release.

name: Deploy Lambda Functions (DEPRECATED)

on:
  # Disabled - use ci-cd.yml instead
  workflow_dispatch:
    inputs:
      confirm:
        description: 'This workflow is deprecated. Use ci-cd.yml instead. Type "confirm" to proceed anyway.'
        required: true
        default: ''

jobs:
  deprecated-warning:
    runs-on: ubuntu-latest
    
    steps:
    - name: Show deprecation warning
      run: |
        echo "=========================================="
        echo "WARNING: This workflow is DEPRECATED"
        echo "=========================================="
        echo ""
        echo "Please use the new CI/CD pipeline instead:"
        echo "  .github/workflows/ci-cd.yml"
        echo ""
        echo "The new pipeline includes:"
        echo "  - Comprehensive testing"
        echo "  - Code quality checks"
        echo "  - Blue-green deployment"
        echo "  - Automatic rollback"
        echo "  - Full infrastructure deployment"
        echo ""
        echo "=========================================="
        exit 1
