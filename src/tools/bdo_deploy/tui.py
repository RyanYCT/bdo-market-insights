"""TUI front-end (interactive mode) for the deploy control plane.

Skeleton only: the Textual application and its guided flows — which render the
``Plan`` at an explicit confirmation step before any mutating execution — are
implemented in task 5.2. A convenience skin over the shared command core, not
the system of record.
"""

from __future__ import annotations


def run_tui() -> int:
    """Launch the Textual application and return the process exit code."""
    raise NotImplementedError("TUI front-end lands in task 5.2")
